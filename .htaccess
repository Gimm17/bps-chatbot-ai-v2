<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Block access to sensitive files
<FilesMatch "^\.env|composer\.(json|lock)|package\.(json|lock)|artisan|\.git">
    Order allow,deny
    Deny from all
</FilesMatch>
